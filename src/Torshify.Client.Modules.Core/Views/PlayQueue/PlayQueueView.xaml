<UserControl
    x:Class="Torshify.Client.Modules.Core.Views.PlayQueue.PlayQueueView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="300">
    <Grid
        Background="Maroon">
        <Grid>
            <DataGrid
                ItemsSource="{Binding Tracks}"
                IsReadOnly="True"
                CanUserAddRows="False"
                CanUserDeleteRows="False"
                AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn
                        Header="Track"
                        Binding="{Binding Track.Name}" />
                    <DataGridTextColumn
                        Header="Artist"
                        Binding="{Binding Track.Album.Artist.Name}" />
                    <DataGridTextColumn
                        Header="Time"
                        Binding="{Binding Track.Duration}" />
                    <DataGridTextColumn
                        Header="Album"
                        Binding="{Binding Track.Album.Name}" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
